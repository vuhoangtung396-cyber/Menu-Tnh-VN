--// TNH-VN KEY SYSTEM FIX

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")

local LP = Players.LocalPlayer
local PlayerGui = LP:WaitForChild("PlayerGui")

-- Xóa GUI cũ nếu còn
pcall(function()
    local old = PlayerGui:FindFirstChild("TNHVN_Key")
    if old then
        old:Destroy()
    end
end)

-- Tạo ScreenGui
local KeyGui = Instance.new("ScreenGui")
KeyGui.Name = "TNHVN_Key"
KeyGui.ResetOnSpawn = false
KeyGui.IgnoreGuiInset = true
KeyGui.DisplayOrder = 999999
KeyGui.Enabled = true
KeyGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
KeyGui.Parent = PlayerGui

-- Hàm tạo object
local function New(className, parent, properties)
    local obj = Instance.new(className)

    for property, value in pairs(properties or {}) do
        pcall(function()
            obj[property] = value
        end)
    end

    obj.Parent = parent
    return obj
end

local function Corner(parent, radius)
    New("UICorner", parent, {
        CornerRadius = UDim.new(0, radius)
    })
end

local function Stroke(parent, color, thickness)
    New("UIStroke", parent, {
        Color = color,
        Thickness = thickness
    })
end

--// KEY FRAME
local KeyFrame = New("Frame", KeyGui, {
    Size = UDim2.new(0, 430, 0, 260),
    Position = UDim2.new(0.5, -215, 0.5, -130),
    BackgroundColor3 = Color3.fromRGB(8, 15, 28),
    BorderSizePixel = 0,
    ZIndex = 10
})

Corner(KeyFrame, 18)

Stroke(
    KeyFrame,
    Color3.fromRGB(0, 130, 255),
    2
)

-- Tiêu đề
New("TextLabel", KeyFrame, {
    Size = UDim2.new(1, -40, 0, 45),
    Position = UDim2.new(0, 20, 0, 18),
    BackgroundTransparency = 1,
    Text = "Tnh-VN Key VIP",
    TextColor3 = Color3.fromRGB(240, 245, 255),
    TextSize = 28,
    Font = Enum.Font.GothamBold,
    TextXAlignment = Enum.TextXAlignment.Left,
    ZIndex = 11
})

New("TextLabel", KeyFrame, {
    Size = UDim2.new(1, -40, 0, 25),
    Position = UDim2.new(0, 20, 0, 58),
    BackgroundTransparency = 1,
    Text = "Nhập Key để tiếp tục",
    TextColor3 = Color3.fromRGB(140, 175, 220),
    TextSize = 16,
    Font = Enum.Font.Gotham,
    TextXAlignment = Enum.TextXAlignment.Left,
    ZIndex = 11
})

-- Ô nhập key
local KeyBox = New("TextBox", KeyFrame, {
    Size = UDim2.new(1, -40, 0, 55),
    Position = UDim2.new(0, 20, 0, 98),
    BackgroundColor3 = Color3.fromRGB(15, 26, 45),
    BorderSizePixel = 0,
    Text = "",
    PlaceholderText = "Dán Key vào đây...",
    PlaceholderColor3 = Color3.fromRGB(100, 135, 180),
    TextColor3 = Color3.fromRGB(230, 240, 255),
    TextSize = 18,
    Font = Enum.Font.Gotham,
    ClearTextOnFocus = false,
    ZIndex = 11
})

Corner(KeyBox, 12)

Stroke(
    KeyBox,
    Color3.fromRGB(0, 125, 255),
    2
)

-- Nút xác nhận
local Confirm = New("TextButton", KeyFrame, {
    Size = UDim2.new(1, -40, 0, 50),
    Position = UDim2.new(0, 20, 0, 168),
    BackgroundColor3 = Color3.fromRGB(0, 115, 255),
    BorderSizePixel = 0,
    Text = "✓  XÁC NHẬN",
    TextColor3 = Color3.fromRGB(255, 255, 255),
    TextSize = 19,
    Font = Enum.Font.GothamBold,
    AutoButtonColor = true,
    ZIndex = 11
})

Corner(Confirm, 12)

-- Thông báo
local Status = New("TextLabel", KeyFrame, {
    Size = UDim2.new(1, -40, 0, 25),
    Position = UDim2.new(0, 20, 0, 225),
    BackgroundTransparency = 1,
    Text = "",
    TextColor3 = Color3.fromRGB(255, 80, 80),
    TextSize = 14,
    Font = Enum.Font.Gotham,
    ZIndex = 11
})

--// CHECK KEY
Confirm.MouseButton1Click:Connect(function()

    local entered = tostring(KeyBox.Text)

    if entered == "Tnh-VN" then

        Status.Text = "✓ Key đúng!"
        Status.TextColor3 = Color3.fromRGB(60, 255, 130)

        task.wait(0.5)

        KeyGui:Destroy()

        -- GỌI MENU CHÍNH Ở ĐÂY
        if CreateMain then
            CreateMain()
        end

    else

        Status.Text = "✕ Key không đúng!"
        Status.TextColor3 = Color3.fromRGB(255, 70, 70)

    end
end)
