--========================================================
-- Tnh-VN DEBUG HUB
-- Dành cho game Roblox do bạn phát triển / kiểm thử
--========================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

local LocalPlayer = Players.LocalPlayer

--========================================================
-- CONFIG
--========================================================

local KEY = "Tnh-VN"

local NORMAL_SPEED = 16
local FAST_SPEED = 50

local NORMAL_JUMP = 50
local HIGH_JUMP = 100

--========================================================
-- STATE
--========================================================

local State = {
    Speed = false,
    HighJump = false,
    HealthBar = false,
    PlayerESP = false,
    GodMode = false,
    Noclip = false,
    AimHead = false,
    WallVision = false,
    Invisible = false
}

local Connections = {}
local ESPObjects = {}

--========================================================
-- UTILITY
--========================================================

local function getCharacter()
    return LocalPlayer.Character
end

local function getHumanoid()
    local character = getCharacter()
    if not character then return nil end
    return character:FindFirstChildOfClass("Humanoid")
end

local function disconnect(name)
    if Connections[name] then
        Connections[name]:Disconnect()
        Connections[name] = nil
    end
end

local function disconnectAll()
    for _, connection in pairs(Connections) do
        pcall(function()
            connection:Disconnect()
        end)
    end

    Connections = {}
end

--========================================================
-- GUI
--========================================================

local Gui = Instance.new("ScreenGui")
Gui.Name = "TnhVNHub"
Gui.ResetOnSpawn = false
Gui.IgnoreGuiInset = true
Gui.Parent = LocalPlayer:WaitForChild("PlayerGui")

--========================================================
-- KEY WINDOW
--========================================================

local KeyFrame = Instance.new("Frame")
KeyFrame.Size = UDim2.fromOffset(600, 380)
KeyFrame.Position = UDim2.fromScale(0.5, 0.5)
KeyFrame.AnchorPoint = Vector2.new(0.5, 0.5)
KeyFrame.BackgroundColor3 = Color3.fromRGB(7, 12, 25)
KeyFrame.BorderSizePixel = 0
KeyFrame.Parent = Gui

local KeyCorner = Instance.new("UICorner")
KeyCorner.CornerRadius = UDim.new(0, 24)
KeyCorner.Parent = KeyFrame

local KeyStroke = Instance.new("UIStroke")
KeyStroke.Thickness = 3
KeyStroke.Color = Color3.fromRGB(20, 120, 255)
KeyStroke.Parent = KeyFrame

-- Rainbow border
local KeyGradient = Instance.new("UIGradient")
KeyGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 140, 255)),
    ColorSequenceKeypoint.new(0.25, Color3.fromRGB(130, 0, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 0, 180)),
    ColorSequenceKeypoint.new(0.75, Color3.fromRGB(0, 255, 200)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 140, 255))
})
KeyStroke.Parent = KeyFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -50, 0, 55)
Title.Position = UDim2.fromOffset(30, 25)
Title.BackgroundTransparency = 1
Title.Text = "Tnh-VN Key VIP"
Title.TextColor3 = Color3.fromRGB(245, 245, 255)
Title.TextSize = 32
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = KeyFrame

local Subtitle = Instance.new("TextLabel")
Subtitle.Size = UDim2.new(1, -60, 0, 35)
Subtitle.Position = UDim2.fromOffset(32, 72)
Subtitle.BackgroundTransparency = 1
Subtitle.Text = "Nhập Key để tiếp tục"
Subtitle.TextColor3 = Color3.fromRGB(140, 175, 230)
Subtitle.TextSize = 18
Subtitle.Font = Enum.Font.Gotham
Subtitle.TextXAlignment = Enum.TextXAlignment.Left
Subtitle.Parent = KeyFrame

local KeyBox = Instance.new("TextBox")
KeyBox.Size = UDim2.new(1, -80, 0, 65)
KeyBox.Position = UDim2.fromOffset(40, 125)
KeyBox.BackgroundColor3 = Color3.fromRGB(9, 19, 38)
KeyBox.BorderSizePixel = 0
KeyBox.PlaceholderText = "🔑  Dán Key vào đây..."
KeyBox.PlaceholderColor3 = Color3.fromRGB(115, 145, 195)
KeyBox.Text = ""
KeyBox.TextColor3 = Color3.fromRGB(230, 240, 255)
KeyBox.TextSize = 21
KeyBox.Font = Enum.Font.Gotham
KeyBox.ClearTextOnFocus = false
KeyBox.Parent = KeyFrame

local KeyBoxCorner = Instance.new("UICorner")
KeyBoxCorner.CornerRadius = UDim.new(0, 18)
KeyBoxCorner.Parent = KeyBox

local KeyBoxStroke = Instance.new("UIStroke")
KeyBoxStroke.Thickness = 2
KeyBoxStroke.Color = Color3.fromRGB(20, 120, 255)
KeyBoxStroke.Parent = KeyBox

local Confirm = Instance.new("TextButton")
Confirm.Size = UDim2.new(1, -80, 0, 70)
Confirm.Position = UDim2.fromOffset(40, 210)
Confirm.BackgroundColor3 = Color3.fromRGB(15, 120, 255)
Confirm.BorderSizePixel = 0
Confirm.Text = "✓   XÁC NHẬN"
Confirm.TextColor3 = Color3.new(1, 1, 1)
Confirm.TextSize = 25
Confirm.Font = Enum.Font.GothamBold
Confirm.Parent = KeyFrame

local ConfirmCorner = Instance.new("UICorner")
ConfirmCorner.CornerRadius = UDim.new(0, 18)
ConfirmCorner.Parent = Confirm

local Info = Instance.new("TextLabel")
Info.Size = UDim2.new(1, -60, 0, 50)
Info.Position = UDim2.fromOffset(30, 300)
Info.BackgroundTransparency = 1
Info.Text = "ⓘ  Key đúng sẽ mở menu và các chức năng kiểm thử."
Info.TextColor3 = Color3.fromRGB(125, 155, 205)
Info.TextSize = 15
Info.Font = Enum.Font.Gotham
Info.Parent = KeyFrame

--========================================================
-- MAIN MENU
--========================================================

local Main = Instance.new("Frame")
Main.Size = UDim2.fromOffset(760, 470)
Main.Position = UDim2.fromScale(0.5, 0.5)
Main.AnchorPoint = Vector2.new(0.5, 0.5)
Main.BackgroundColor3 = Color3.fromRGB(5, 8, 18)
Main.BorderSizePixel = 0
Main.Visible = false
Main.Parent = Gui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 18)
MainCorner.Parent = Main

local MainStroke = Instance.new("UIStroke")
MainStroke.Thickness = 3
MainStroke.Color = Color3.fromRGB(0, 140, 255)
MainStroke.Parent = Main

--========================================================
-- HEADER
--========================================================

local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 65)
Header.BackgroundColor3 = Color3.fromRGB(8, 13, 28)
Header.BorderSizePixel = 0
Header.Parent = Main

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 18)
HeaderCorner.Parent = Header

local Logo = Instance.new("TextLabel")
Logo.Size = UDim2.fromOffset(45, 45)
Logo.Position = UDim2.fromOffset(12, 10)
Logo.BackgroundColor3 = Color3.fromRGB(20, 110, 255)
Logo.Text = "T"
Logo.TextColor3 = Color3.new(1, 1, 1)
Logo.TextSize = 28
Logo.Font = Enum.Font.GothamBold
Logo.Parent = Header

local LogoCorner = Instance.new("UICorner")
LogoCorner.CornerRadius = UDim.new(1, 0)
LogoCorner.Parent = Logo

local HubTitle = Instance.new("TextLabel")
HubTitle.Size = UDim2.new(1, -150, 1, 0)
HubTitle.Position = UDim2.fromOffset(70, 0)
HubTitle.BackgroundTransparency = 1
HubTitle.Text = "MENU CHO TẤT CẢ GAME"
HubTitle.TextColor3 = Color3.new(1, 1, 1)
HubTitle.TextSize = 21
HubTitle.Font = Enum.Font.GothamBold
HubTitle.TextXAlignment = Enum.TextXAlignment.Left
HubTitle.Parent = Header

--========================================================
-- MINIMIZE / CLOSE
--========================================================

local Minimize = Instance.new("TextButton")
Minimize.Size = UDim2.fromOffset(45, 45)
Minimize.Position = UDim2.new(1, -100, 0, 10)
Minimize.BackgroundColor3 = Color3.fromRGB(20, 25, 40)
Minimize.Text = "−"
Minimize.TextColor3 = Color3.new(1, 1, 1)
Minimize.TextSize = 28
Minimize.Font = Enum.Font.GothamBold
Minimize.Parent = Header

local MinCorner = Instance.new("UICorner")
MinCorner.CornerRadius = UDim.new(1, 0)
MinCorner.Parent = Minimize

local Close = Instance.new("TextButton")
Close.Size = UDim2.fromOffset(45, 45)
Close.Position = UDim2.new(1, -50, 0, 10)
Close.BackgroundColor3 = Color3.fromRGB(35, 18, 30)
Close.Text = "×"
Close.TextColor3 = Color3.fromRGB(255, 100, 120)
Close.TextSize = 28
Close.Font = Enum.Font.GothamBold
Close.Parent = Header

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(1, 0)
CloseCorner.Parent = Close

--========================================================
-- LEFT MENU
--========================================================

local Left = Instance.new("ScrollingFrame")
Left.Size = UDim2.fromOffset(210, 390)
Left.Position = UDim2.fromOffset(10, 72)
Left.BackgroundColor3 = Color3.fromRGB(8, 12, 24)
Left.BorderSizePixel = 0
Left.ScrollBarThickness = 3
Left.CanvasSize = UDim2.new()
Left.Parent = Main

local LeftCorner = Instance.new("UICorner")
LeftCorner.CornerRadius = UDim.new(0, 12)
LeftCorner.Parent = Left

local Layout = Instance.new("UIListLayout")
Layout.Padding = UDim.new(0, 7)
Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
Layout.Parent = Left

local Padding = Instance.new("UIPadding")
Padding.PaddingTop = UDim.new(0, 10)
Padding.PaddingBottom = UDim.new(0, 10)
Padding.Parent = Left

--========================================================
-- RIGHT CONTENT
--========================================================

local Content = Instance.new("ScrollingFrame")
Content.Size = UDim2.new(1, -235, 0, 390)
Content.Position = UDim2.fromOffset(225, 72)
Content.BackgroundColor3 = Color3.fromRGB(8, 12, 24)
Content.BorderSizePixel = 0
Content.ScrollBarThickness = 4
Content.CanvasSize = UDim2.new()
Content.Parent = Main

local ContentCorner = Instance.new("UICorner")
ContentCorner.CornerRadius = UDim.new(0, 12)
ContentCorner.Parent = Content

local ContentLayout = Instance.new("UIListLayout")
ContentLayout.Padding = UDim.new(0, 8)
ContentLayout.Parent = Content

local ContentPadding = Instance.new("UIPadding")
ContentPadding.PaddingTop = UDim.new(0, 12)
ContentPadding.PaddingLeft = UDim.new(0, 12)
ContentPadding.PaddingRight = UDim.new(0, 12)
ContentPadding.PaddingBottom = UDim.new(0, 12)
ContentPadding.Parent = Content

--========================================================
-- ESP
--========================================================

local function clearESP()
    for _, obj in pairs(ESPObjects) do
        pcall(function()
            obj:Destroy()
        end)
    end

    ESPObjects = {}
end

local function createESP(player)
    if player == LocalPlayer then return end

    local character = player.Character
    if not character then return end

    local root = character:FindFirstChild("HumanoidRootPart")
    if not root then return end

    local highlight = Instance.new("Highlight")
    highlight.Name = "TnhVN_ESP"
    highlight.FillTransparency = 0.75
    highlight.OutlineTransparency = 0
    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    highlight.Parent = character

    table.insert(ESPObjects, highlight)
end

local function updateESP()
    clearESP()

    if not State.PlayerESP and not State.WallVision then
        return
    end

    for _, player in ipairs(Players:GetPlayers()) do
        createESP(player)
    end
end

--========================================================
-- HEALTH BAR
--========================================================

local function healthBar(player)
    if player == LocalPlayer then return end

    local character = player.Character
    if not character then return end

    local head = character:FindFirstChild("Head")
    local humanoid = character:FindFirstChildOfClass("Humanoid")

    if not head or not humanoid then return end

    local old = head:FindFirstChild("TnhVNHealth")
    if old then old:Destroy() end

    local billboard = Instance.new("BillboardGui")
    billboard.Name = "TnhVNHealth"
    billboard.Size = UDim2.fromOffset(100, 12)
    billboard.StudsOffset = Vector3.new(0, 3, 0)
    billboard.AlwaysOnTop = true
    billboard.Parent = head

    local background = Instance.new("Frame")
    background.Size = UDim2.fromScale(1, 1)
    background.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    background.BorderSizePixel = 0
    background.Parent = billboard

    local fill = Instance.new("Frame")
    fill.Size = UDim2.fromScale(
        math.clamp(humanoid.Health / humanoid.MaxHealth, 0, 1),
        1
    )
    fill.BackgroundColor3 = Color3.fromRGB(0, 220, 100)
    fill.BorderSizePixel = 0
    fill.Parent = background

    local connection
    connection = humanoid.HealthChanged:Connect(function(hp)
        if not fill.Parent then
            connection:Disconnect()
            return
        end

        fill.Size = UDim2.fromScale(
            math.clamp(hp / humanoid.MaxHealth, 0, 1),
            1
        )
    end)

    table.insert(Connections, connection)
end

local function updateHealthBars()
    for _, player in ipairs(Players:GetPlayers()) do
        if State.HealthBar then
            healthBar(player)
        end
    end
end

--========================================================
-- SPEED
--========================================================

local function updateSpeed()
    local humanoid = getHumanoid()
    if not humanoid then return end

    humanoid.WalkSpeed = State.Speed and FAST_SPEED or NORMAL_SPEED
end

--========================================================
-- HIGH JUMP
--========================================================

local function updateJump()
    local humanoid = getHumanoid()
    if not humanoid then return end

    humanoid.UseJumpPower = true
    humanoid.JumpPower = State.HighJump and HIGH_JUMP or NORMAL_JUMP
end

--========================================================
-- NOCLIP
--========================================================

local function updateNoclip()
    disconnect("Noclip")

    if not State.Noclip then
        return
    end

    Connections.Noclip = RunService.Stepped:Connect(function()
        local character = getCharacter()

        if not character then return end

        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end)
end

--========================================================
-- INVISIBILITY
--========================================================

local function updateInvisible()
    local character = getCharacter()
    if not character then return end

    for _, obj in ipairs(character:GetDescendants()) do
        if obj:IsA("BasePart") then
            obj.LocalTransparencyModifier =
                State.Invisible and 1 or 0
        elseif obj:IsA("Decal") then
            obj.Transparency =
                State.Invisible and 1 or 0
        end
    end
end

--========================================================
-- GOD MODE - LOCAL DEBUG
--========================================================

local function updateGodMode()
    local humanoid = getHumanoid()
    if not humanoid then return end

    disconnect("GodMode")

    if State.GodMode then
        Connections.GodMode = humanoid.HealthChanged:Connect(function()
            if State.GodMode and humanoid.Health < humanoid.MaxHealth then
                humanoid.Health = humanoid.MaxHealth
            end
        end)

        humanoid.Health = humanoid.MaxHealth
    end
end

--========================================================
-- TOGGLE
--========================================================

local function toggleFeature(name)
    State[name] = not State[name]
end

--========================================================
-- BUTTON CREATION
--========================================================

local buttons = {}

local function createToggle(text, stateName, callback)
    local button = Instance.new("TextButton")

    button.Size = UDim2.new(1, -20, 0, 48)
    button.BackgroundColor3 = Color3.fromRGB(18, 22, 38)
    button.BorderSizePixel = 0
    button.Text = ""
    button.Parent = Content

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 10)
    corner.Parent = button

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -70, 1, 0)
    label.Position = UDim2.fromOffset(15, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(230, 235, 250)
    label.TextSize = 16
    label.Font = Enum.Font.GothamMedium
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = button

    local toggle = Instance.new("Frame")
    toggle.Size = UDim2.fromOffset(42, 22)
    toggle.Position = UDim2.new(1, -55, 0.5, -11)
    toggle.BackgroundColor3 = Color3.fromRGB(55, 60, 75)
    toggle.Parent = button

    local toggleCorner = Instance.new("UICorner")
    toggleCorner.CornerRadius = UDim.new(1, 0)
    toggleCorner.Parent = toggle

    local dot = Instance.new("Frame")
    dot.Size = UDim2.fromOffset(16, 16)
    dot.Position = UDim2.fromOffset(3, 3)
    dot.BackgroundColor3 = Color3.fromRGB(220, 225, 235)
    dot.Parent = toggle

    local dotCorner = Instance.new("UICorner")
    dotCorner.CornerRadius = UDim.new(1, 0)
    dotCorner.Parent = dot

    local function refresh()
        if State[stateName] then
            toggle.BackgroundColor3 = Color3.fromRGB(0, 190, 100)
            dot.Position = UDim2.fromOffset(23, 3)
        else
            toggle.BackgroundColor3 = Color3.fromRGB(55, 60, 75)
            dot.Position = UDim2.fromOffset(3, 3)
        end
    end

    button.MouseButton1Click:Connect(function()
        toggleFeature(stateName)

        if callback then
            callback(State[stateName])
        end

        refresh()
    end)

    refresh()

    buttons[stateName] = {
        Button = button,
        Refresh = refresh
    }

    return button
end

--========================================================
-- LEFT CATEGORY BUTTON
--========================================================

local function createCategory(text)
    local button = Instance.new("TextButton")

    button.Size = UDim2.new(1, -20, 0, 42)
    button.BackgroundColor3 = Color3.fromRGB(12, 17, 31)
    button.BorderSizePixel = 0
    button.Text = text
    button.TextColor3 = Color3.fromRGB(210, 220, 240)
    button.TextSize = 15
    button.Font = Enum.Font.GothamBold
    button.Parent = Left

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 9)
    corner.Parent = button

    return button
end

createCategory("⚡  Movement")
createCategory("👁  Visual")
createCategory("🎯  Combat")
createCategory("🛡  Player")
createCategory("⚙  Settings")

--========================================================
-- FEATURES
--========================================================

createToggle("Tăng tốc độ chạy", "Speed", function()
    updateSpeed()
end)

createToggle("Nhảy cao", "HighJump", function()
    updateJump()
end)

createToggle("Hiển thị thanh máu", "HealthBar", function()
    updateHealthBars()
end)

createToggle("Định vị người chơi", "PlayerESP", function()
    updateESP()
end)

createToggle("Bất tử", "GodMode", function()
    updateGodMode()
end)

createToggle("Đi xuyên tường", "Noclip", function()
    updateNoclip()
end)

createToggle("Nhìn xuyên tường", "WallVision", function()
    updateESP()
end)

createToggle("Tàng hình", "Invisible", function()
    updateInvisible()
end)

createToggle("Tự động ngắm đầu", "AimHead", function(enabled)
    -- Điểm nối cho hệ thống Aim Assist của game.
    -- Không tự động điều khiển camera nếu chưa cấu hình target.
end)

--========================================================
-- CHARACTER RESPAWN
--========================================================

LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1)

    updateSpeed()
    updateJump()
    updateNoclip()
    updateInvisible()
    updateGodMode()

    if State.PlayerESP or State.WallVision then
        task.wait(0.5)
        updateESP()
    end

    if State.HealthBar then
        updateHealthBars()
    end
end)

--========================================================
-- MINIMIZED LOGO
--========================================================

local SmallLogo = Instance.new("TextButton")
SmallLogo.Size = UDim2.fromOffset(58, 58)
SmallLogo.Position = UDim2.fromOffset(20, 250)
SmallLogo.BackgroundColor3 = Color3.fromRGB(5, 5, 8)
SmallLogo.Text = "T"
SmallLogo.TextColor3 = Color3.new(1, 1, 1)
SmallLogo.TextSize = 30
SmallLogo.Font = Enum.Font.GothamBold
SmallLogo.Visible = false
SmallLogo.Parent = Gui

local SmallCorner = Instance.new("UICorner")
SmallCorner.CornerRadius = UDim.new(1, 0)
SmallCorner.Parent = SmallLogo

local SmallStroke = Instance.new("UIStroke")
SmallStroke.Thickness = 3
SmallStroke.Color = Color3.fromRGB(30, 120, 255)
SmallStroke.Parent = SmallLogo

SmallLogo.MouseButton1Click:Connect(function()
    SmallLogo.Visible = false
    Main.Visible = true
end)

--========================================================
-- DRAG SYSTEM
--========================================================

local function makeDraggable(frame, handle)
    local dragging = false
    local dragStart
    local startPos

    handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
            or input.UserInputType == Enum.UserInputType.Touch then

            dragging = true
            dragStart = input.Position
            startPos = frame.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if not dragging then return end

        if input.UserInputType == Enum.UserInputType.MouseMovement
            or input.UserInputType == Enum.UserInputType.Touch then

            local delta = input.Position - dragStart

            frame.Position = UDim2.new(
                startPos.X.Scale,
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
        end
    end)
end

makeDraggable(Main, Header)
makeDraggable(SmallLogo, SmallLogo)

--========================================================
-- MINIMIZE
--========================================================

Minimize.MouseButton1Click:Connect(function()
    Main.Visible = false
    SmallLogo.Visible = true
end)

--========================================================
-- CLOSE
--========================================================

Close.MouseButton1Click:Connect(function()

    -- Tắt toàn bộ chức năng
    for name in pairs(State) do
        State[name] = false
    end

    disconnectAll()
    clearESP()

    local character = getCharacter()

    if character then
        for _, obj in ipairs(character:GetDescendants()) do
            if obj:IsA("BasePart") then
                obj.LocalTransparencyModifier = 0
                obj.CanCollide = true
            end
        end
    end

    local humanoid = getHumanoid()

    if humanoid then
        humanoid.WalkSpeed = NORMAL_SPEED
        humanoid.JumpPower = NORMAL_JUMP
    end

    Gui:Destroy()
end)

--========================================================
-- KEY CHECK
--========================================================

Confirm.MouseButton1Click:Connect(function()

    if KeyBox.Text == KEY then
        KeyFrame.Visible = false
        Main.Visible = true
    else
        KeyBox.Text = ""
        KeyBox.PlaceholderText = "❌ Key không đúng!"
        KeyBoxStroke.Color = Color3.fromRGB(255, 60, 80)

        task.delay(1.5, function()
            if KeyBox.Parent then
                KeyBox.PlaceholderText = "🔑  Dán Key vào đây..."
                KeyBoxStroke.Color = Color3.fromRGB(20, 120, 255)
            end
        end)
    end
end)
